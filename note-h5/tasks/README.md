# tasks

This directory stores Harness task snapshots generated during execution.

Each `task-*.json` file records one user request, its task status, checkpoints, errors, timestamps, and execution metadata.

These files are runtime records, not source code. They can be deleted when resetting the demo environment.
