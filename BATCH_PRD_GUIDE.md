# 批量处理 PRD 指南

当你的需求包含多个功能时，可以使用批量执行脚本。

## 🚀 快速开始

### 1. 创建 PRD 文件

创建一个文本文件，每行一个需求：

```bash
cat > my-prd.txt << EOF
# 用户管理功能
增加用户注册功能
增加用户登录功能
增加密码重置功能

# 笔记核心功能
增加置顶笔记的功能
增加笔记归档功能
增加笔记搜索功能
EOF
```

### 2. 执行批量任务

```bash
./batch-harness.sh my-prd.txt
```

## 📋 PRD 文件格式

PRD 文件支持以下格式：

```
# 注释行（以 # 开头）会被忽略

# 功能分组（使用注释）
# 核心功能
增加置顶笔记的功能
增加笔记归档功能

# 空行会被自动跳过

# 编辑功能
增加笔记富文本编辑功能
增加笔记图片上传功能
```

## 📊 执行示例

```bash
$ ./batch-harness.sh example-prd.txt

============================================================
🚀 批量执行 Harness 任务
============================================================
PRD 文件: example-prd.txt

============================================================
任务 1/6: 增加置顶笔记的功能
============================================================
...
✅ 成功

============================================================
任务 2/6: 增加笔记归档功能
============================================================
...
✅ 成功

...

============================================================
📊 执行总结
============================================================
总任务: 6
成功: 5
失败: 1

⚠️  有 1 个任务失败，请检查日志
```

## 🎯 高级用法

### 并行执行（慎用）

如果你有多个独立功能，可以并行执行：

```bash
# 后台执行所有任务
for req in $(cat my-prd.txt | grep -v '^#'); do
    ./harness.sh "$req" &
done
wait
```

### 断点续传

创建一个记录已完成的脚本：

```bash
#!/bin/bash
PRD_FILE="$1"
COMPLETED_FILE="${PRD_FILE}.done"

while IFS= read -r requirement; do
    if [[ -z "$requirement" || "$requirement" =~ ^# ]]; then
        continue
    fi

    # 检查是否已完成
    if grep -qF "$requirement" "$COMPLETED_FILE" 2>/dev/null; then
        echo "⏭  跳过: $requirement"
        continue
    fi

    echo "执行: $requirement"
    if ./harness.sh "$requirement"; then
        echo "$requirement" >> "$COMPLETED_FILE"
    fi
done < "$PRD_FILE"
```

## 📝 PRD 编写建议

### 好的 PRD 描述

- ✅ "增加置顶笔记的功能"
- ✅ "实现笔记搜索，支持按标题和内容搜索"
- ✅ "添加用户头像上传功能，支持裁剪"

### 不好的 PRD 描述

- ❌ "做笔记功能"（太模糊）
- ❌ "fix bug"（不具体）
- ❌ "优化性能"（范围太广）

## 🔍 查看执行日志

批量执行时，每个任务的日志保存在 `/tmp/harness-$$.log`：

```bash
# 查看特定任务的日志
cat /tmp/harness-12345.log
```

## 📈 进度跟踪

执行完成后，可以查看所有任务的追踪：

```bash
# 按时间排序显示所有追踪
ls -lt trace/ | head -20

# 查看最近的成功任务
grep -l '"status": "completed"' trace/*.json | head -10

# 查看最近的失败任务
grep -l '"status": "failed"' trace/*.json | head -10
```
