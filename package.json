{
  "name": "@befe/harness",
  "version": "0.1.0",
  "description": "前端代码治理框架 — 分层架构校验、命名规范、依赖边界管控",
  "private": true,
  "scripts": {
    "test": "cd .harness && .venv/bin/python -m pytest tests/ -q",
    "test:full": "cd .harness && .venv/bin/python -m pytest tests/ -v"
  },
  "engines": {
    "python": ">=3.9"
  }
}
