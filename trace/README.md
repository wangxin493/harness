# trace

This directory stores Harness execution traces generated during a run.

Each `trace-*.json` file keeps structured step events, and each `trace-*.md` file is the readable trace report for the same execution.

These files are used for debugging, reviewing Agent behavior, and explaining how Planner, Coder, Reviewer, and build verification interacted during a task.
